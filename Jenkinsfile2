pipeline {
    agent any 
    stages {
        stage ('build-in-env') {
            steps {
                sh '''
                    echo "Build url is : $BUILD_URL"
                    echo "Build number is : $BUILD_NUMBER"
                '''
            }
        }
    }
}
